{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Home Assistant (dev)",
      "type": "debugpy",
      "request": "launch",
      "module": "homeassistant",
      "args": ["--config", "${env:HA_CONFIG_DIR}", "--debug"],
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      },
      "justMyCode": false
    }
  ]
}
